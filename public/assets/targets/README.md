Place your compiled MindAR target file here.

Expected typical files:

- `public/assets/targets/your-target.mind`
- optional preview image for internal reference

The current default config uses the MindAR sample target hosted remotely so the MVP can boot immediately.

